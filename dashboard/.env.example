# Python backend target used by the Vite development proxy
VITE_BACKEND_TARGET=http://127.0.0.1:8000

# Set this for cross-origin production deployments; leave empty for same-origin
VITE_API_BASE_URL=
